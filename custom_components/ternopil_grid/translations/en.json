{
  "config": {
    "step": {
      "user": {
        "title": "Ternopil Grid Schedule",
        "description": "Choose a street from the searchable list and optionally enter a house number.",
        "data": {
          "street_id": "Street",
          "house_number": "House number"
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Options",
        "menu_options": {
          "street": "Change street",
          "ping": "Probe behavior",
          "ping_entity": "Device probe target",
          "ping_manual": "Manual IP target",
          "resolver": "DHCP / lease resolver",
          "diagnostics": "Diagnostics & logging"
        }
      },
      "street": {
        "title": "Change street",
        "description": "Choose a new street from the searchable list and optionally update the house number.",
        "data": {
          "street_id": "Street",
          "house_number": "House number"
        }
      },
      "diagnostics": {
        "title": "Diagnostics & logging",
        "description": "Enable verbose ternopil_grid debug logging when you need live troubleshooting.",
        "data": {
          "debug_logging": "Enable verbose debug logging"
        }
      },
      "ping": {
        "title": "Probe behavior",
        "description": "Configure whether the live power probe is enabled, how it checks for power, and how often it runs.",
        "data": {
          "ping_enabled": "Enable live power probe",
          "ping_method": "Probe method",
          "ping_interval": "Check every (s)",
          "ping_timeout": "Timeout (s)",
          "ping_history_hours": "Probe history (hours)"
        }
      },
      "ping_entity": {
        "title": "Device probe target",
        "description": "Choose the Home Assistant device or entity that should be used as the live power probe target.",
        "data": {
          "ping_entity_id": "Probe device",
          "ping_port": "Port",
          "ping_http_ssl": "Use HTTPS for HTTP check",
          "ping_http_path": "HTTP path"
        }
      },
      "ping_manual": {
        "title": "Manual IP target",
        "description": "Enter the IP address used as the live power probe target.",
        "data": {
          "ping_ip": "Target IP address",
          "ping_port": "Port",
          "ping_http_ssl": "Use HTTPS for HTTP check",
          "ping_http_path": "HTTP path"
        }
      },
      "resolver": {
        "title": "DHCP / lease resolver",
        "description": "Optionally resolve the probe target from an external lease source. Resolution order is entity IP, then DHCP/lease data, then the saved fallback IP.",
        "data": {
          "ping_dhcp_source": "Lease source",
          "ping_dhcp_lookup": "Lookup strategy",
          "ping_dhcp_target": "Lookup override",
          "ping_dhcp_url": "Lease JSON URL",
          "ping_dhcp_file": "Lease JSON file path"
        }
      }
    }
  },
  "error": {
    "invalid_target_source": "Entity status method requires a probe device/entity target.",
    "invalid_ip": "Enter a valid IP address.",
    "invalid_url": "Enter a valid http:// or https:// URL.",
    "invalid_mac": "Enter a valid MAC address.",
    "entity_requires_explicit_fallback": "This device has no IP in Home Assistant. Re-save Manual IP target for this device or enable the DHCP / lease resolver first.",
    "entity_missing_ip": "The selected device does not expose a usable IP address."
  }
}
